name: DEPRECATED — Use .github/workflows/firebase-hosting-deploy.yml

on:
  # This workflow is intentionally disabled to avoid duplicate deploys.
  # Kept only for history/reference. It will never trigger by itself.
  workflow_call:

jobs: {}
